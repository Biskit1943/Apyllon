Starts playing the next Song (if available)
---
tags:
  - Player
parameters:
  - in: body
    name: body
    required: true
    schema:
      $ref: "#/definitions/Username"
responses:
  200:
    description: The next song
    schema:
      $ref: "#/definitions/Song"
  404:
    description: No previous song
