Set repeat
---
tags:
  - Player
parameters:
  - in: body
    name: body
    required: true
    schema:
      $ref: "#/definitions/username"
responses:
  200:
    type: object
    properties:
      state:
        type: boolean
        description: True for repeat on and false for off
        example: false
