Uploads files to the server
---
tags:
  - Local
consumes:
  - multipart/form-data
parameters:
  - in: formData
    name: files
    type: file
    description: The file(s) to upload.
responses:
  200:
    description: Success
